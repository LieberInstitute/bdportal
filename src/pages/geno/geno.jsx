import { h } from 'preact';
import './style.css';

const GenoPages = ( {tab} ) => (
	<div>
		<h1>Genotyping Data</h1>
		<p>Query and explore available genotyping data.</p>
	</div>
);

export default GenoPages;
