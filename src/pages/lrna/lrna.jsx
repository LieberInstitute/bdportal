import { h } from 'preact';
import './style.css';

const LongRnaPages = ( {tab} ) => (
	<div>
		<h1>Long RNA-seq Data</h1>
		<p>Query and explore available long read RNA-seq data.</p>
	</div>
);

export default LongRnaPages;
