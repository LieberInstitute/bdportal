import { h } from 'preact';
import './style.css';

const ScRnaPages = ( {tab} ) => (
	<div>
		<h1>scRNA Data</h1>
		<p>Query and explore available scRNA data.</p>
	</div>
);

export default ScRnaPages;
